# Railway Deployment Guide

## خطوات نشر المشروع على Railway

### 1. إنشاء حساب على Railway
- اذهب إلى [railway.app](https://railway.app)
- قم بإنشاء حساب أو تسجيل الدخول

### 2. ربط مستودع GitHub
- من لوحة التحكم في Railway، اختر "New Project"
- اختر "Deploy from GitHub"
- قم بربط حساب GitHub الخاص بك
- اختر المستودع `EL-Akeil`

### 3. تكوين المشروع
- Railway سيكتشف تلقائياً `Dockerfile` و `railway.json`
- تأكد من أن المتغيرات البيئية مضبوطة:

```
FLASK_ENV=production
FLASK_DEBUG=0
SECRET_KEY=your_secure_key_here
DATABASE_URL=sqlite:///data.db  # أو قاعدة بيانات خارجية
PORT=5000
HOST=0.0.0.0
```

### 4. متغيرات البيئة المهمة

في لوحة تحكم Railway، اضبط:

| المتغير | القيمة | الملاحظة |
|-------|--------|---------|
| `FLASK_ENV` | `production` | بيئة الإنتاج |
| `FLASK_DEBUG` | `0` | تعطيل وضع التصحيح |
| `SECRET_KEY` | `your_key` | مفتاح آمن قوي |
| `PORT` | `5000` | المنفذ (Railway يعينه تلقائياً) |

### 5. نشر المشروع
- اضغط على "Deploy" في Railway
- انتظر حتى ينتهي البناء والنشر (~2-3 دقائق)

### 6. الوصول إلى التطبيق
- سيحصل تطبيقك على رابط فريد من Railway
- مثال: `https://el-akeil-production.railway.app`

### 7. مراقبة السجلات
- من لوحة التحكم، اختر "Logs" لمراقبة الخادم

## ملفات التكوين المستخدمة

- **Dockerfile**: يحدد بيئة التشغيل وطريقة بناء الصورة
- **railway.json**: تكوين Railway
- **.dockerignore**: الملفات التي يجب تجاهلها
- **Procfile**: تعليمات تشغيل العملية (اختياري مع Docker)

## استكشاف الأخطاء

### المشكلة: الخادم لا يبدأ
- تحقق من السجلات في Railway
- تأكد من أن جميع المتغيرات البيئية مضبوطة
- تحقق من أن `requirements.txt` محدث

### المشكلة: قاعدة البيانات لا تعمل
- إذا كنت تستخدم SQLite، فقد تحتاج إلى قاعدة بيانات خارجية (PostgreSQL)
- اضبط `DATABASE_URL` بشكل صحيح

### المشكلة: الملفات الثابتة لا تحمل
- تأكد من أن مجلد `Frontend` موجود وقابل للقراءة
- تحقق من أن `send_from_directory` يشير إلى المسار الصحيح

## نصائح إضافية

1. **استخدم قاعدة بيانات خارجية**: لا تستخدم SQLite في الإنتاج
2. **اضبط SECRET_KEY**: استخدم مفتاح قوي وآمن
3. **فعّل HTTPS**: Railway يوفره بشكل افتراضي
4. **مراقب الأداء**: استخدم لوحة تحكم Railway لمراقبة الاستخدام

