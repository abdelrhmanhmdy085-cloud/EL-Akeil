# 🗺️ Chef Location Map - شريحة سريعة للاستخدام

## ما الذي تم إضافته؟

تمت إضافة نظام خرائط متقدم يتضمن:

### 1️⃣ خريطة العميل (Customer Map)
**الموقع:** `src/Frontend/customer.html`
- عرض الشيفات القريبين من موقعك
- اختيار شيف من الخريطة مباشرة
- عرض المسافة والتقييم والوقت

### 2️⃣ خريطة التحليلات (Analytics Map)
**الموقع:** `src/Frontend/analytics.html`
- عرض جميع الشيفات على الخريطة
- Heatmap يوضح المناطق الساخنة (عالية الطلب)
- إحصائيات حية عن المنصة
- المناطق المزدحمة

---

## 🚀 كيفية التشغيل

### الخطوة 1: تحديث قاعدة البيانات
تأكد من أن `ChefProfile` يحتوي على موقع:

```python
chef = ChefProfile.query.first()
chef.lat = 30.0444  # خط العرض (مثال)
chef.long = 31.2357  # خط الطول (مثال)
chef.address = "عنوان الشيف"
db.session.commit()
```

### الخطوة 2: تشغيل Backend
```bash
cd src/backend
python app.py
```

### الخطوة 3: فتح الموقع
```
http://localhost:5000/customer.html     # لصفحة العميل
http://localhost:5000/analytics.html    # لصفحة التحليلات
```

---

## 📍 استخدام الخريطة

### من صفحة العميل:
1. **وافق على مشاركة موقعك** عند طلب المتصفح
2. **انظر للشيفات** على الخريطة حولك
3. **اختر شيف** بالنقر عليه
4. **شاهد أطعمته** وأكمل الطلب

### من صفحة التحليلات:
1. **شاهد الخريطة الكاملة** لجميع الشيفات
2. **فعّل الـ Heatmap** لرؤية المناطق الساخنة
3. **اضغط "Show Busy Areas"** لرؤية المناطق المزدحمة
4. **شاهد الإحصائيات** في الجانب الأيمن

---

## 🎨 الألوان على الخريطة

| اللون | المعنى |
|-------|--------|
| 🔵 أزرق | موقعك |
| 🟡 ذهبي | شيف ممتاز (4.5+) |
| 🟠 برتقالي | شيف جيد (4+) |
| 🔴 أحمر | شيف عادي |
| 🟢 أخضر | كوريير نشط |

---

## 📊 البيانات المطلوبة

### لكل شيف:
```python
{
    "lat": 30.0444,          # خط العرض
    "long": 31.2357,         # خط الطول
    "address": "العنوان",
    "prep_time_avg": 30,     # دقائق
    "rating": 4.5            # من التقييمات
}
```

### لكل طلب مكتمل (للـ Heatmap):
```python
{
    "customer_lat": 30.0500,
    "customer_long": 31.2400,
    "status": "delivered"
}
```

---

## 🔧 API Endpoints المتاحة

```bash
# جلب الشيفات
GET /api/customer/chefs?lat=30.0444&long=31.2357&max_distance=50

# جلب الأطعمة
GET /api/customer/dishes

# جلب بيانات الخريطة الحرارية
GET /api/customer/heatmap-data

# جلب الإحصائيات
GET /api/customer/chef-analytics

# جلب المناطق المزدحمة
GET /api/customer/busy-areas

# إنشاء طلب
POST /api/customer/order
{
    "chef_id": 1,
    "items": [{"dish_id": 1, "qty": 1, "price": 50}]
}
```

---

## ⚙️ المتطلبات الإضافية

### JavaScript Libraries:
- ✅ Leaflet (للخرائط)
- ✅ Leaflet Heat (للـ Heatmap)
- ✅ Socket.IO (للتحديثات الحية)

جميعها محملة من CDN بالفعل ✔️

---

## 🐛 استكشاف الأخطاء

### الخريطة لا تظهر:
- تحقق من أن العنصر `<div id="map">` موجود
- تحقق من اتصال الإنترنت

### الشيفات لا يظهرون:
- تأكد من أن `ChefProfile` يحتوي على `lat` و `long`
- اختبر الـ API: `GET /api/customer/chefs`

### الموقع لا يُطلب:
- قد يكون المتصفح منع الطلب
- جرّب في المتصفح الخاص بك وافق على الطلب

---

## 📁 الملفات الجديدة

```
src/Frontend/
├── analytics.html                    # صفحة التحليلات
├── assets/js/
│   ├── chefs-map.js                 # خريطة الشيفات الأساسية
│   └── advanced-map.js              # خريطة التحليلات المتقدمة
└── customer.html                    # محدثة مع الخريطة

src/backend/
├── routes/
│   └── customer.py                  # محدث مع API endpoints جديدة
├── models.py                         # محدث مع تحسينات
└── app.py                            # محدث مع URL prefixes
```

---

## 🎯 الخطوات التالية

1. ✅ قم بتحديث بيانات الشيفات بالمواقع
2. ✅ اختبر الخريطة من صفحة العميل
3. ✅ اختبر صفحة التحليلات
4. ✅ فعّل تتبع الكوريير في الوقت الفعلي
5. ✅ أضف تنبيهات المناطق المزدحمة

---

## 📞 الدعم

للمزيد من المعلومات، راجع:
- `CHEF_LOCATION_MAP_DOCUMENTATION.md` - التوثيق الكامل
- Backend APIs في `src/backend/routes/customer.py`
- Frontend code في `src/Frontend/assets/js/`

---

**آخر تحديث:** 21 يناير 2026
**الإصدار:** 1.0
**الحالة:** ✅ جاهز للاستخدام
