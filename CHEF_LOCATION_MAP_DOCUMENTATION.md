# خريطة الشيفات - Chef Location Map Feature

## نظرة عامة
تم إضافة نظام متقدم لعرض مواقع الشيفات على خرائط Leaflet مع ميزات متقدمة تشمل:
- خريطة تفاعلية بموقع الشيفات والعملاء
- Heatmap لعرض المناطق ذات الطلب العالي
- تتبع الكوريير في الوقت الفعلي
- تحليلات شاملة عن نشاط المنصة
- عرض المناطق المزدحمة

## الملفات المضافة/المحدثة

### 1. Frontend Files

#### `src/Frontend/assets/js/chefs-map.js` (ملف جديد)
ملف JavaScript يحتوي على وظائف الخريطة الأساسية:
- `initChefMap()` - تهيئة الخريطة مع موقع المستخدم
- `loadChefsOnMap()` - جلب الشيفات وعرضهم على الخريطة
- `addChefMarker()` - إضافة علامة شيف على الخريطة
- `selectChef()` - تحديد شيف وتصفية الأطعمة
- `fitMapToBounds()` - ضبط حدود الخريطة
- `recenterMapOnUser()` - إعادة تمركز الخريطة على موقع المستخدم

#### `src/Frontend/assets/js/advanced-map.js` (ملف جديد)
ملف JavaScript متقدم للتحليلات والتتبع:
- `initAdvancedChefMap()` - تهيئة خريطة التحليلات
- `setupRealtimeTracking()` - إعداد تتبع الكوريير في الوقت الفعلي
- `updateDriverMarker()` - تحديث موقع الكوريير
- `displayHeatmap()` - عرض Heatmap للطلب
- `getChefAnalytics()` - جلب إحصائيات الشيفات
- `displayBusyAreas()` - عرض المناطق المزدحمة
- `exportHeatmapImage()` - حفظ الخريطة كصورة

#### `src/Frontend/customer.html` (محدث)
تحديثات:
- دمج سكريبت `chefs-map.js`
- تحسين عرض الخريطة مع زر إعادة التمركز
- إضافة معلومات عن الشيفات القريبة
- تحسين نموذج الأطعمة لعرض معلومات الشيف
- إضافة وظيفة `filterByChef()` لتصفية الأطعمة حسب الشيف المحدد
- إضافة رابط للذهاب إلى صفحة التحليلات

#### `src/Frontend/analytics.html` (ملف جديد)
صفحة متقدمة لعرض:
- خريطة كاملة مع جميع الشيفات والمناطق
- Heatmap للمناطق ذات الطلب العالي
- إحصائيات حية (عدد الشيفات النشطين، الطلبات اليومية، التقييم)
- قائمة الشيفات القريبة
- المناطق المزدحمة (حرجة، عالية، متوسطة)
- خيارات للتحكم والتصدير

### 2. Backend Files

#### `src/backend/routes/customer.py` (محدث)
مسارات API جديدة:
- `GET /customer/chefs` - جلب قائمة الشيفات مع المواقع والتقييمات
  - معاملات اختيارية: `lat`, `long`, `max_distance`
  - يرجع المسافة من موقع المستخدم (إن وجد)

- `GET /customer/dishes` - جلب قائمة الأطعمة المتاحة
  - معاملات اختيارية: `category`, `food_level`, `occasion_tag`

- `POST /customer/order` - إنشاء طلب جديد
  - معاملات: `chef_id`, `items`
  - ترسل إخطار للشيف عند إنشاء الطلب

- `GET /customer/orders` - جلب طلبات المستخدم

- `POST /customer/review` - إضافة تقييم ومراجعة للشيف

- `GET /customer/heatmap-data` - جلب بيانات Heatmap
  - يرجع آخر 1000 طلب مكتمل مع مواقعهم

- `GET /customer/chef-analytics` - جلب إحصائيات الشيفات
  - إجمالي عدد الشيفات
  - عدد الشيفات النشطين (آخر 24 ساعة)
  - عدد الطلبات اليومية
  - متوسط التقييم

- `GET /customer/busy-areas` - جلب المناطق المزدحمة
  - معرفة الطلب العالي
  - عدد الطلبات في كل منطقة
  - عدد الشيفات النشطين في كل منطقة

وظائف مساعدة:
- `haversine_distance()` - حساب المسافة بين نقطتين على الأرض

#### `src/backend/models.py` (محدث)
تحديثات:
- تحسين `ChefProfile.to_dict()` لإضافة موقع الشيف
- تحسين `Dish.to_dict()` لإضافة `user_id` الشيف والتقييم

#### `src/backend/app.py` (محدث)
- تعديل URL prefix للـ customer blueprint من `/api` إلى `/api/customer`

## ميزات الخريطة

### 1. صفحة العميل (customer.html)
**عرض المواقع:**
- الخريطة تعرض موقع المستخدم بعلامة زرقاء
- الشيفات يظهرون بعلامات ملونة (ذهبي/برتقالي/أحمر) حسب التقييم
- معلومات الشيف في popup عند النقر على العلامة:
  - الاسم والعنوان
  - التقييم وعدد المراجعات
  - متوسط وقت التحضير
  - المسافة من المستخدم

**التصفية حسب الموقع:**
- البحث التلقائي عن موقع المستخدم (Geolocation API)
- إظهار فقط الشيفات في نطاق 50 كم
- ترتيب الشيفات حسب المسافة
- زر لإعادة تمركز الخريطة على موقع المستخدم

**تفاعل المستخدم:**
- النقر على الشيف يعرض الأطعمة الخاصة به
- تصفية الأطعمة حسب الشيف المحدد
- تحديث الخريطة لتمييز الشيف المختار

### 2. صفحة التحليلات (analytics.html)
**الخريطة الرئيسية:**
- عرض جميع الشيفات النشطين
- تتبع الكوريير في الوقت الفعلي
- Heatmap توضح المناطق الساخنة (عالية الطلب)

**الإحصائيات:**
- إجمالي عدد الشيفات
- عدد الشيفات النشطين الآن
- عدد الطلبات اليومية
- متوسط التقييم العام

**المناطق المزدحمة:**
- عرض المناطق ذات الطلب العالي بدوائر ملونة
- تصنيف المناطق: حرجة (أحمر)، عالية (برتقالي)، متوسطة (أصفر)
- معلومات عن عدد الطلبات والشيفات النشطين في كل منطقة

**التحكم:**
- تبديل الـ Heatmap
- ضبط حدود الخريطة على جميع العلامات
- حفظ الخريطة كصورة

## متطلبات البيانات

### البيانات المطلوبة في قاعدة البيانات:

1. **ChefProfile يجب أن تحتوي:**
   - `lat` - خط العرض
   - `long` - خط الطول
   - `address` - العنوان
   - `prep_time_avg` - متوسط وقت التحضير

2. **Dish يجب أن تحتوي:**
   - `chef_profile_id` - معرف الشيف
   - `category` - فئة الطعام
   - `prep_time` - وقت التحضير

3. **Order يجب أن تحتوي:**
   - `customer_lat` - خط عرض العميل
   - `customer_long` - خط طول العميل
   - `status` - حالة الطلب

## التحقق من التثبيت الصحيح

### 1. فحص API endpoints:
```bash
# جلب قائمة الشيفات
GET /api/customer/chefs

# جلب الأطعمة
GET /api/customer/dishes

# جلب Heatmap data
GET /api/customer/heatmap-data

# جلب الإحصائيات
GET /api/customer/chef-analytics

# جلب المناطق المزدحمة
GET /api/customer/busy-areas
```

### 2. اختبار في Frontend:
- افتح `customer.html` في المتصفح
- تحقق من ظهور الخريطة
- تحقق من طلب الموقع
- تحقق من ظهور الشيفات على الخريطة
- انقر على رابط Analytics للذهاب للصفحة المتقدمة
- تحقق من ظهور الـ Heatmap والإحصائيات

### 3. المتطلبات اللازمة:
- بيانات الموقع (lat/long) في Chef profiles
- Leaflet Library (محمل بالفعل من CDN)
- Leaflet Heat Plugin (للـ Heatmap)
- Socket.IO للإخطارات والتحديثات الحية

## الاستخدام

### للعملاء:
1. **من صفحة العميل:**
   - افتح customer.html
   - السماح بالوصول إلى موقعك
   - اختر شيف من الخريطة
   - اختر الأطعمة التي تريدها
   - أكمل عملية الطلب

2. **من صفحة التحليلات:**
   - افتح analytics.html
   - شاهد الخريطة الكاملة للمنصة
   - فعّل الـ Heatmap لرؤية المناطق الساخنة
   - اضغط على الشيفات من القائمة الجانبية
   - شاهد الإحصائيات الحية

### لإدارة البيانات:
تأكد من أن ChefProfile يحتوي على:
```python
chef.lat = 30.0444  # مثال
chef.long = 31.2357
chef.address = "عنوان الشيف"
```

وأن Order يحتوي على:
```python
order.customer_lat = 30.0500
order.customer_long = 31.2400
```

## الملاحظات الهامة

1. **الموقع الجغرافي:** يتطلب موافقة المستخدم على مشاركة الموقع
2. **الخطأ:** إذا لم يشارك المستخدم الموقع، ستستخدم الخريطة موقع افتراضي (القاهرة)
3. **الأداء:** الحد الأقصى للشيفات المعروضة لا يتجاوز 50 كم
4. **الألوان:**
   - أزرق: موقع المستخدم
   - ذهبي: تقييم 4.5+
   - برتقالي: تقييم 4+
   - أحمر: تقييم أقل من 4
   - أخضر: كوريير نشط
   - Heatmap: من أزرق (بارد) إلى أحمر (ساخن)

## المميزات المستقبلية المقترحة

1. عرض الشيفات ذوي التقييم العالي أولاً
2. تصفية متقدمة حسب نوع الطعام والمسافة
3. عرض وقت التوصيل المتوقع
4. خيارات للتكبير/التصغير الذكي
5. تحديث موقع الكوريير في الوقت الفعلي على الخريطة
6. تنبيهات المناطق المزدحمة
7. توقعات الطلب بناءً على البيانات التاريخية
8. تقارير مفصلة عن أداء الشيفات
